# PULSE API Error Envelope v1

Canonical error response. During the 90-day transition, `error_legacy` and `fields` are compatibility fields. `error.request_id` must equal the response `X-Request-ID`.
