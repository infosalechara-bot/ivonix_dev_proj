apiVersion: v1
kind: ConfigMap
metadata: {name: pulse-config}
data:
  cors-origins: "https://app.example.com"
---
apiVersion: v1
kind: Secret
metadata: {name: pulse-secrets}
type: Opaque
stringData:
  DATABASE_URL: "REPLACE"
  DATABASE_USERNAME: "REPLACE"
  DATABASE_PASSWORD: "REPLACE"
  SUPABASE_URL: "REPLACE"
  SUPABASE_PUBLISHABLE_KEY: "REPLACE"
  PULSE_JWT_SECRET: "REPLACE_WITH_32_BYTE_MINIMUM_SECRET"
  VIRUSTOTAL_API_KEY: ""
